/**
 * Barrel for the shared domain types.
 * Import from `@/types` rather than reaching into deep paths.
 */
export type {
  Flashcard,
  QuizQuestion,
  StudyContent,
  GenerationKind,
  UserProfile,
  DailyUsage,
  StudySet,
} from "./index";
